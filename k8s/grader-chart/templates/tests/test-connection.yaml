apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grader-chart.fullname" . }}-test-connection"
  labels:
    {{- include "grader-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grader-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
